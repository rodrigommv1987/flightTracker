class Destination {

    constructor({city, airportCode, direct}) {
        this.city = city;
        this.airportCode = airportCode;
        this.direct = direct;
    }

}

module.exports = Destination;